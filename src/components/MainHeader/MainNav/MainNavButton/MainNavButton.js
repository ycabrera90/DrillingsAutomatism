import React from "react";
import classes from "./MainNavButton.module.css";

const MainNavButton = () => {
  return (
    <button className={classes["main-nav__button"]}>
      <span>YC</span>
    </button>
  );
};

export default MainNavButton;
